# Scripts

本目录用于放置可复现实验脚本。建议脚本具备以下特点：

- 支持命令行参数；
- 支持读取 `configs/` 中的配置文件；
- 不写死本地绝对路径；
- 输出到 `outputs/` 或 `data/interim/`、`data/processed/`。

后续可添加：

- 数据转换脚本；
- ASR 批量转写脚本；
- 错误类型统计脚本；
- 下游任务评估脚本。
